## What

<!-- Short description: what this PR changes and why. -->

## How verified

- [ ] **C / CMake / tests** — e.g. `verify-incremental.bat` (Windows) or `bash scripts/ci-build.sh` as appropriate
- [ ] **Add-on catalog** — N/A, or `python scripts/generate_addon_catalog.py --check` passed (hub sibling or `./synastria-monorepo-addons/`; use `--input` / `AHC_HUB_ADDONS_JSON` if nonstandard)
- [ ] **Android** — N/A, or `android-verify` / `scripts/android-verify.sh` passed
- [ ] **Manual / UI** — N/A, or [docs/user-testing.md](../docs/user-testing.md) where behavior changed

## Notes for reviewers

<!-- Optional: trade-offs, follow-ups, or links to issues. -->

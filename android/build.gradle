// Placeholder: wire CMake from a consumer app module. Touch defines: AHC_ANDROID, optional AHC_BUILD_APP=0.

plugins {
    id 'base'
}

// No Java/Kotlin sources in this repository stub. Consumer apps should:
//  externalNativeBuild { cmake { path file('../android/CMakeLists.txt') } }
//  defaultConfig { externalNativeBuild { cmake { arguments "-DANDROID_STL=c++_shared" } } }

group = "io.github.attunehelper"
version = "0.0.0-stub"

task touchDefines {
    doLast {
        println "AHC_ANDROID: define in CMake for C sources (see android/CMakeLists.txt)."
    }
}

defaultTasks "touchDefines"
